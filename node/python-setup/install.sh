# cd C:/Users/kalawants/Desktop/pythonBash
# python3 -m 
# python3 -m venv venv
# venv\Scripts\activate.bat

#!/bin/bash
#npm i python
npm i python@0.0.3
# mkdir my_project
# cd my_project
#command will install the latest version of a module and its dependencies from the Python Package
python -m pip install SomePackage
# if a suitable module is already installed,Upgrading existing modules must be requested if needed
python -m pip install --upgrade SomePackage
#way to run Python scripts from Node.js 
npm i python-shell
pip install django
pip install request
pip install "fastapi[all]"
pip install "uvicorn[standard]"
pip install -r requirements.txt
python -m pip install fastapi uvicorn[standard]
npm install shelljs @types/shelljs
# django-main.py startproject samplesite
# npm install typescript --save-dev
npm init -y
npm i mariadb